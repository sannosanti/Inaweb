  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title text-center"  >Documentos</h3>
    </div>
    <table class="table table-hover">
    <thead>
      <tr>
        <th>Titulo</th>
        <th>Categoria</th>
        <th>Descripcion</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @documents.each do |document| %>
        <tr>
          <td><%= document.titulo %></td>
          <td><%= get_categoria(document.categoria) %></td>
          <td><%= document.descripcion %></td>
          <td class= "text-center">
            <%= link_to document, class:'btn btn-default btn-sm' do %>
              <i class="fa fa-eye fa-2x"></i>
            <% end %>
          </td>         
          <td class= "text-center">
          <%= link_to edit_document_path(document), class: 'btn btn-default btn-sm'  do %>     
            <i class="fa fa-pencil fa-2x" ></i>
          </td>
          <% end %>

          <td class= "text-center"><%= link_to document, class:'btn btn-default btn-sm', method: :delete, data: { confirm: 'Are you sure?' } do %>
              <i class="fa fa-trash fa-2x"></i>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  </div>

  <br>
<div class= "text-center"><%= link_to 'Nuevo Documento', new_document_path, class:'btn btn-default' %></div>
  